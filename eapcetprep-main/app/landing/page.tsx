'use client'

// Landing route: reuse the paywall-style Landing B experience at `/landing`.
// The classic marketing landing continues to live at `/` (app/page.tsx).
export { default } from '../landing-b/page'

